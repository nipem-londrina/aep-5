/*
const { conn } = require("../database/connection")

class Autarquia {

    /*constructor(id, nomeCampo, vagasTotais ,endereco) {
        this.id = id
        this.nome = nomeCampo
        this.vagasTotais = vagasTotais
        this.endereco = endereco
    }

    cadastrarLocalEstagio() {
        let sql = `INSERT INTO localEstagio (id, nomeCampo, vagasTotais, endereco)
                    VALUES(
                        ${this.id},
                        '${this.nomeCampo},'
                        ${this.vagasTotais},
                        '${this.endereco}'

                    )`
        conn.execute(sql)
    }

    cadastrarIES(){
        let sql = `INSERT INTO insituicaoDeEnsino ()
                    VALUES(
                        ${this.id},

                    )`
        conn.execute(sql)
    }

    autorizar() {

    }

    reprovar() {

    }
*//*
}

module.exports = { Autarquia }

*/